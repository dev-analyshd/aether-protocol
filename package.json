{
  "name": "aether-protocol",
  "version": "1.0.0",
  "description": "AETHER — The Behavioral Clearing Network for BOT Chain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "deploy:bohr": "hardhat run scripts/deploy.ts --network bohr_testnet",
    "node": "hardhat node",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.0",
    "ethers": "^6.9.0"
  }
}
